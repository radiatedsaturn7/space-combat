package com.spacecombat;

public class DestroyOnOutOfBounds extends Component 
{
	public update ()
	{
		if (gameObject.transform.position.y > 812u)
		{
			gameObject.destroy();
		}
		
		if (gameObject.transform.position.y < 128)
		{
			gameObject.destroy();
		}
	}
}
